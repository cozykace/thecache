# ── NEVER COMMIT THESE ──────────────────────────────
# Secrets: bank tokens, API keys, anything sensitive
.env
.env.*
*.secret
.simplefin

# Your actual financial data — stays on YOUR machine only
data/
*.sqlite
*.sqlite3
*.db
transactions*.json
accounts*.json
# local backups of your data — stay on your machine
backups/
# ────────────────────────────────────────────────────

# macOS noise
.DS_Store
*.log

# Python
__pycache__/
*.pyc

# Toggl API token (credential) — never commit
.toggl

# PostHog Personal API key (read access) — founder only, never commit
.posthog

# Founder lock — presence of this file marks THIS machine as King Cozy.
# Never commit: it's what keeps founder-only surfaces personal to you.
.founder

# Internal planning docs — private, local only (decided: PRINCIPLES public, the rest internal)
Working Docs/2_STRUCTURE.md
Working Docs/3_ROADMAP.md
Working Docs/Meetings/

# Brain Bucket — Cozy's actively-held working memory for dev sessions. Arbitrary personal
# files land here so every Claude session reads them; NEVER commit any of it.
Working Docs/Brain Bucket/

# Brand/design references — private, local only (in Working Docs/ with hierarchy prefix)
Working Docs/5_BRAND.md

# Cloud architecture plan — private security/strategy doc (in Working Docs/ with hierarchy prefix)
Working Docs/4_CLOUD-PLAN.md

# WebDAV backup target (url/user/pass) — local only, never commit
.webdav
Working Docs/7_STRATEGY.md

# Internal company wiki — business/strategy/infra context base. PRIVATE, never commit.
Working Docs/WIKI/
