# THE CACHE — working conventions

A calm, private **life OS** built for people with executive function challenges (ADHD, autism, TBI). Money is the first mature area, not the product. Plain HTML/CSS/JS served by a Python stdlib backend. No build step, no framework.

**Read `Working Docs/1_PRINCIPLES.md` (the WHY) and `Working Docs/2_STRUCTURE.md` (the WHAT) first — then this doc for code conventions. Then check `Working Docs/Brain Bucket/` — anything in there is what Cozy is actively holding in his head right now; read every file in it and treat the contents as live context for the session.**

## Hard rules (do not break)

1. **Privacy first.** Real bank data — counterparty names, dollar amounts, account numbers — never goes into committed code, comments, docstrings, test fixtures, or the chat transcript. Use placeholders (`Jane Doe`), counts, initials, or booleans. Everything under `data/` is gitignored and stays on this machine; never commit it.
   **⚠️ THE WHOLE REPO IS PUBLIC** (`cozykace/thecache`, visibility: PUBLIC). Not just the roadmap — **every source file, every commit message, the entire git history, and `Working Docs/1_PRINCIPLES.md` + `6_PROJECT-MANAGER.md`** are readable by anyone. Assume anything you commit is published the moment it's pushed. The ONLY private places are the gitignored ones: `data/`, `Working Docs/Brain Bucket/`, `Working Docs/2_STRUCTURE.md`, `3_ROADMAP.md`, `4_CLOUD-PLAN.md`, `5_BRAND.md`, `7_STRATEGY.md`, and the dotfile secrets. **Business strategy, governance, legal posture, personal-security notes, and anything Cozy flags as private go in `Working Docs/7_STRATEGY.md` (gitignored) — NEVER in BACKLOG/FEATURES/commit messages.** (The LICENSE is proprietary — "All rights reserved" — so visibility ≠ permission. Repo-visibility strategy: see `7_STRATEGY.md`.)
2. **Lightweight above all.** No build step, no framework, no bundler. Plain `.html`/`.css`/`.js` + Python stdlib. If a change wants a dependency, find another way first. (We deliberately abandoned React/Vite — it overloaded the machine.)
3. **Beauty is non-negotiable.** Apple-clean but a little alien / early-internet. Never trade visual quality for utility.
4. **Brick by brick.** Build on the real working base, one small piece at a time. No big speculative rewrites.
5. **No user left behind — always plan the migration path.** Every change that touches stored data, account/auth, the vault, encryption, or any `money.*` key MUST answer "what happens to people who are already on the old version?" before it ships. Existing data keeps opening; existing accounts keep working; old formats migrate-on-read (never break, never destructively rewrite) and upgrade on the next deliberate write. This is not optional polish — a founder account was locked out of a v1 legacy vault (2026-07-25) precisely because an old scheme had no forward path to the recovery net. When in doubt, add a migration + a test that loads the OLD shape and proves it still works. The keybox's "legacy `m:esc`/`m:zk` open forever" and the deck's tombstone-synthesis migration are the models.

## Architecture

- `index.html` — page shell (sidebar, board, status bar). Loads `app.js` plus locally-vendored Lucide + Motion One (no CDNs — security eval T4), all guarded so the app still works if one fails. **No custom cursor** — the OS cursor is the only cursor (removed 2026-07-17: a JS-drawn cursor drifts from the real pointer and fights accessibility software).
- `app.js` — the engine. `RENDERERS` (how each widget type draws), `LIBRARY` (singleton widgets you toggle), drag/resize, magnet snap, theme, modals.
- `styles.css` — all styling. Themed via CSS vars; `[data-theme]` palettes.
- `store.py` — shared data layer: categorize/income logic, `build_snapshot`, atomic writes (`os.replace`), permanent ledger, daily history/backups.
- `sync.py` — SimpleFIN bank pull. `server.py` — serves the dashboard + small JSON write APIs (bound to 127.0.0.1).
- `data/` — local JSON (balances, transactions, categories, income, ledger, history). Gitignored.

## Conventions

- **Icons: use Lucide** (`<i data-lucide="name"></i>` then call `drawIcons()`). Don't hand-draw SVG icons. Verify a name exists before using it.
- **Theme-aware colors only.** Use CSS vars — `var(--ink)`, `var(--paper)`, `rgba(var(--ink-rgb), a)`, `var(--accent)`, `var(--edge-soft)`. Never hardcode black/white (breaks dark mode).
- **Data flow.** Widgets `fetch("data/balances.json")`. Editing data (categories, income) POSTs to a `server.py` endpoint that calls a `recompute_*` and rewrites `balances.json`; the widget re-fetches.
- **Persistence.** UI state → `localStorage` (keys namespaced `money.*`). Layout → `saveLayout()`. Backend writes are atomic.
- **Shared helpers in app.js:** `fmtUSD`, `fmtUSDk`, `windowRange`, `escapeHtml`, `incomeBubbles`, `drawIcons`, `springIn`. Reuse them.
- **D3 is vendored + lazy-loaded** for the Visualizer (the trip → `openLedger`), never a CDN, injected on first open with a graceful fallback — see the WIKI SOP (`Working Docs/WIKI/30-infrastructure/d3-sop.md`).

## Mobile-friendly (SOP)

The board is desktop-first today (drag / zoom / pan), but build everything new so it survives a phone. Apply by default:

1. **Touch targets ≥ 44×44px.** Buttons, toggles, pips, slider thumbs, close ×. If the glyph must stay small (a 16px ×), pad the *hit area* to ~44px with padding/`::before`, not the glyph.
2. **No hover-only anything.** Controls revealed on `:hover` (sticker magnet/×, row actions) are invisible on touch — mirror every reveal with `@media (hover: none){ … opacity:1 }`. Never put essential info only in a `title=` tooltip.
3. **Fluid, not fixed.** Size with `%`, `fr`, `clamp()`, container units (`cqmin`) — never hardcoded desktop px widths. Every flex child holding text/inputs gets `min-width:0`; lean on the global `box-sizing:border-box`. A widget must never overflow its own box at any size (the body is `overflow:hidden`). Inputs/sliders: `width:100%` + pad chunky controls by their own radius so the thumb can't poke past the edge when the widget shrinks.
4. **Container queries over media queries.** Widgets adapt to their OWN width (`container-type:size` is on `.widget`), so a narrow widget == a narrow phone. **Shrinking a widget is your mobile test** since I can't see the screen.
5. **`touch-action` on gestures.** Draggables/resizers `touch-action:none`; sliders/scroll regions `pan-y`/`manipulation`. We already use Pointer Events — keep it, don't add mouse-only listeners.
6. **Thumb-friendly inputs.** `type="number"` + `inputmode="decimal"` for money, `type="date"` for dates → correct mobile keyboard. Prefer inline editors over `prompt()` for anything new.
7. **Modals → bottom sheets under ~480px.** Fixed desktop popovers (settings, pickers, period/clock menus) become full-width, bottom-anchored, scrollable sheets; always tap-outside + visible-close dismissible.
8. **Readable minimums.** Body/number text ≥ 12–13px; never let an 8–9px label be the only carrier of essential info.
9. **Respect safe areas.** Anything fixed/full-bleed (dock, stats bar, sheets) pads with `env(safe-area-inset-*)` so notches/home bars don't clip it.
10. **Board needs an edit mode (deferred).** Drag-to-move + pinch-zoom fight one-finger scroll on touch. Until there's an explicit arrange toggle (or long-press-to-drag), treat the board as view-mostly on phones — widgets must be useful without rearranging.
11. **Verify at a real width.** Ask the user to check ~375px (iPhone) and at the widget's container breakpoints. Lightweight still rules — no heavy reflow/JS on scroll.

## Data contract

**`data/balances.json`** (the snapshot every widget reads):
- top: `updated`, `rev`, `total`, `cash`, `burn_per_day`, `spend_window_days`
- **`updated` vs `rev` — keep these distinct.** `updated` is the BANK-SYNC timestamp (feeds "synced X ago"). `rev` is a counter bumped by EVERY derived-data change — `_next_rev()` in `recompute_spending`/`recompute_income`/`save_balances`, so a categorize, income tag, delete-txn, CSV import, or a tag merged from another device all move it. `period_summary` surfaces both. The client's `dataStamp(d)` = `updated|rev` is what widgets key their side-feed re-pulls on (Money Map, income-forecast history, money-flow transfers) AND what arms the cloud auto-push. **Never bump `updated` on a recompute** — a local tag edit would masquerade as a fresh bank sync.
- `spending`: `{ window_days, total, per_month, per_day, trend_pct, categories:[{key, amount}] }`
- `income`: `{ window_days, total, per_month, sources:[{source, key, amount, tagged}], untagged }`
- `subscriptions`: `{ window_days, total, per_month, items:[{name, key, amount, count, descriptions[], accounts[]}] }`
- `accounts`: `[{id, name, org, balance, currency}]`

**Other data files** (`data/`, all gitignored): `transactions.json` (30d window `{updated, window_days, transactions:[{id, posted, amount, description, account}]}`), `ledger.json` (permanent `{key: txn}`), `monthly.json` (`{updated, months:[{ym, label, income, spending, net, count, live, imported, categories}]}`), `coverage.json` (`{updated, accounts:[{account, count, first, last, live, imported, source}], live_first, live_last, total}`), `categories.json` (`{substring: category}`), `income.json` (`{source_key: "income"|"ignore"}`), `deleted.json` (**tombstones** — `{txn_key: {deleted:1|0, at, txn:{…}}}`; `merge_ledger` is THE choke point for anything entering the ledger and skips tombstoned keys, so a delete survives a sync from a device that still holds it AND a restore from an older vault; the full txn is kept so `undelete_txn` can put it back, and `deleted:0` is an explicit un-delete that outranks an older tombstone on merge. Adopting a tombstone/undo in `merge_maps` also APPLIES it to the local ledger — otherwise a delete would only ever take effect on the machine that made it), `bugs.json` (`[{id, text, status, created, solved?}]`), `history.json`, `synclog.json`, `_mapmeta.json` (per-key edit times `{filename: {key: mtime_ms}}` for the four merge maps — categories/income/subs/income_links; leading `_` keeps it out of the export files bundle, it rides the vault as `filesMeta` and drives `merge_maps`' newest-per-key cross-device merge).

**API** (`server.py`, 127.0.0.1) — GET: `ping` `connect-status` `summary` `categories` `recurring` `transfers` `deposits` `merchants` `other-merchants` `averages` `work` `income-monthly` `work-monthly` `integrity` `issues` `subs` `income-links` `match-count` `bugs` `deleted` (the undo list) `update-check`. POST: `categorize` `income` `category` `sync` `import` `import-data` `merge-maps` (cross-device newest-per-key merge of the EIGHT user-authored files — categories/income/subs/income_links + **catmeta** + **deleted** (tombstones) + **manual_accounts** (typed balances, Brick 4 — removal is a `{removed:1}` VALUE so deletes propagate; an absent key can't) + **account_roles** (liquid/short/long/untouchable per account id — EXACT-case keys, never lowercased: they're ids, not merchant substrings; "untouchable"+"long" feed the client-side `acctSetAside` → Safe-to-spend/runway/portal spendable) (renames newest-per-key, custom categories unioned+SORTED — an unsorted union list would livelock the authored-hash witness) — via the vault's `filesMeta` sidecar; recomputes if it changed anything, incl. `recompute_manual` so an adopted manual-balance edit moves Total. `import_data` routes these through the same merge instead of replacing them) `manual-account` (create/update/remove a typed-balance account → `recompute_manual` moves Total/cash NOW) `bug` `bug-status` `subs` `income-links` `delete-txn` (leaves a tombstone) `undelete-txn` (undo) `connect` `update` `restart`.

**localStorage registry** (all `money.*`) — keep CURRENT when you add a key:
- *board/layout*: `layout.v2` `zoom` `gutter` `views` `pinned` `sidebar` `sidebarWidth` `icons.collapsed` `dockHidden` `dockOrder` `dockMobile` (per-device "show the dock on this phone" opt-in — excluded from the vault snapshot like cloudKey/deviceId) `statsScroll` (remembered stats-strip swipe position)
- *identity/character*: `profile` `cacheName` `founder` `charLog` `charSince` `customStats` `statsHidden` `statsOrder` `bugCredits` (fixed-report credits `[{id, at, exp}]` — the idempotency set behind "you reported it, it got fixed, you earned EXP"; SPECIAL union by report id, and the EXP grant banks into a deterministic `expBy["bug:<id>"]` ledger slot so two devices seeing the same fix converge on ONE grant) `profileCard` (the EDITABLE identity card — pronouns/bio + the public display-name opt-in `{publicName, shareName}` — **GENERIC** on purpose: never widen `money.profile`'s EXP witness with profile text; the only field that ever goes public is the display name, via `profilePublicPayload` → the `profiles` row, and only while `shareName` is on)
- *look*: `theme` `themeStars` `bg` `font` `menuTier` `privacy`
- *money config*: `reserve` `need` `core` `guaranteedIncome` `rate` `rent` `rentAccount` `period` `mustpayOrder` `planNextOpen` `balExpanded` `balNet`
- *subs/categories*: `subcore` `subnames` `subpaused` `subcadence` `subsMigrated` `cats` `catMgr` `catModal` `flowCards`
- *income forecast*: `forecastSources` `forecastGoal` `forecastMode`
- *misc/ui*: `favorites` `autoPinFavorites` `soundtrack` `clock24` `clockSecs` `dateFmt` `tz` `note` `timer` (work/rest PRESETS — syncs) `timerRun` (the running countdown: phase/cycle/endsAt/pausedLeft — DEVICE-LOCAL, never synced, or a block running on the desktop would hijack the phone) `skipUpdate` `settings` `connect` `badges` `deckCoach` (one-time deck coaching card seen) `actionTaps` (normalized tap positions on the action button — heat-map raw material, capped 1000) `calview` (calendar default view + week-start + month-grid density — GENERIC, synced so the calendar looks the same on every device) `deckDay` (the deck's currently-viewed day — DEVICE-LOCAL, siloed per device; the deck is a date scroller that restores the exact last-viewed day on reopen) `justReset` (**DEVICE-LOCAL** — the one-shot password-reset confetti flag, consumed on the next login. It was GENERIC and therefore rode the vault, throwing a "you reset your password!" party on devices that never reset anything: transient one-shot flags must never sync) `waterfx` (**DEVICE-LOCAL** — set to `"0"` to switch off the living-water board background; a per-device rendering choice like `dockMobile`, so a low-power phone can opt out without darkening the desktop) `sessionRun` (**DEVICE-LOCAL** — which session the timer is focused on, a per-device runtime pointer like `timerRun`)
- *cloud*: `cloud` (account/session state) `cloudKey` (the device's vault data key — never rides in the encrypted bundle; excluded by snapshotLocal/restoreLocal) `cloudPaused` (local-only toggle) `deviceId` (the EXP-ledger slot id — also never rides the bundle) `__lmeta` (per-key merge bookkeeping `{key:{m,h}}` — local-only, excluded from the vault; NEVER rename the double-underscore prefix without updating both runtimes)
- *brand kit (founder-only)*: `brandkit` (the device MIRROR of the COMPANY cache's shared brand kit — palette/type/logos/notes/tokens + attributed activity log — **DEVICE_LOCAL**, re-pulled on open like `dms`; NOT the founder's own vault data, it's the company's) `brandkitApply` (per-device "apply the kit's tokens to my app" toggle — **DEVICE_LOCAL**). The kit itself lives in a dedicated **company-cache account's vault**, its data-key ECDH-wrapped to each founder (`t:"ecdh"` keybox wrap); see the memory `brandkit-widget-pending-merge` + `Working Docs/Brain Bucket/brandkit-SETUP.md`.
- *social/messages*: `social` (`{username, optedIn}` — your public @handle, GENERIC so it syncs) `msgKey` (`{priv, pub}` JWK — your ECDH identity keypair; GENERIC so it rides the ENCRYPTED vault and your identity follows you across devices) `dms` (the message cache: per-thread decrypted messages + read marks + friend-request cache — **DEVICE_LOCAL**, a mirror of server data, never rides the vault) `notifs` (`{id: {read:0|1, at:ms}}` — read state for the Cache's own news (the notification center) — **SPECIAL**: per-id newest-wins by `at`, exact-`at` tie → unread wins; stamps are DETERMINISTIC — seed/detect stamp the entry's RELEASE date, only a real user read/unread stamps `Date.now()` — so devices converge in any merge order and a fresh device's read-seed can never swallow an unread). Cleared on account-switch in `cloudLogin` so a new login can't inherit the prior account's handle/key.

**Vault merge classes** (per-key sync — `snapshotLocal`/`mergeRemoteLocal`/`stampGeneric`/`buildLocalMeta` in app.js, mirrored in webcache.js `pullVault`; the two runtimes MUST use the same djb2 + same key lists or phone/desktop fork). Every `money.*` key is exactly one of:
- **INTERNAL** — never rides the vault. Two lists (both excluded): `CLOUD_INTERNAL_KEYS` = `cloud` `cloudKey` `cloudPaused` `deviceId` `__lmeta` (cloud/identity); `DEVICE_LOCAL_KEYS` = `dockMobile` `zoom` `gutter` `sidebar` `sidebarWidth` `statsScroll` `icons.collapsed` `balExpanded` `settings` `connect` `wiki` `timerRun` `deckDay` `dms` `simplefin` `sessionRun` `justReset` `waterfx` `brandkit` `brandkitApply` (device-ergonomic geometry + per-device view state + the messages cache + the browser bank credential + one-shot/render flags + the founder brand-kit mirror — pinned per device so the phone never snaps to desktop-pixel layout, each device keeps its own place in the deck's day-scroller, the DM cache is a device-local mirror of server data, `sessionRun`/`justReset`/`waterfx` are per-device runtime/one-shot flags, **`simplefin` is the SimpleFIN Access URL, a bearer secret to the user's bank — device-local so it never rides the vault, the browser twin of the desktop's chmod-600 `.simplefin`**, and **`brandkit`/`brandkitApply` mirror the COMPANY cache's shared kit — the kit lives in that account's OWN vault, ECDH-wrapped to each founder, never in the founder's own vault**). webcache uses one combined `W_INTERNAL` with the same members. **⚠️ `dms`, `simplefin`, AND `sessionRun` are the three exceptions in `isAccountDataKey`/`wIsAccountData`: internal (out of the vault) BUT account-data, so they're siloed under `cacheprof.<uid>` and cleared on logout/switch — without that, a shared browser lets one account read the previous account's messages, pull its bank, or bank timer minutes into its session.** `brandkit` is deliberately NOT in that exception list (it's the COMPANY's shared kit, not per-account; the renderer clears the mirror for non-founders on a shared browser). See WIKI `40-decisions/2026-07-24-bank-credential-device-only`.
- **SPECIAL** (`SPECIAL_MERGE_KEYS`, own merge, never plain LWW): `log`/`logPending` (union) · `deck` (**PER-ITEM merge** — see below) · `charLog` (union) · `profile` (EXP-ledger aggregate) · `badges` (union ids) · `customStats` (union tapped marks per id) · `charSince` (min = earliest founding date) · `notifs` (per-id newest-wins by `at`, tie → unread wins — NOT a union: a union of read ids could never express "mark unread again"; `mergeNotifsStr` ⇄ webcache `wMergeNotifs`, witness-projected to normalized `{at, read}` in `_authoredProject` — see `tests/notifs_test.js`) · `bugCredits` (union by report id — the fixed-report credit set; witness projects to id+exp since `at` is keep-local). **`deckRev` is RETIRED** (now in INTERNAL): per-item `updated` replaced it. Leaving a key un-merged but still in `snapshotLocal`/`authoredHash` is a guaranteed livelock — two converged devices hash differently forever.
- **GENERIC** (everything else — layout, look, config, note, forecast, cats, subnames, `timer`…): per-key newest-wins by an mtime in `__lmeta`. A newly-seen key claims `m:0` (shipping code never wins a merge); a real edit (content hash changed) claims `Date.now()`. The vault blob carries a sibling `localMeta:{genericKey:mtime}` next to the flat `local`; **old vaults lack it → read as all-mtime-0 (migrate-on-read), so the change is backward-compatible**. When you add a `money.*` key, decide its class: an accumulator (never-lose) → SPECIAL with a merger; pure per-device presentation → `DEVICE_LOCAL_KEYS`; otherwise it's GENERIC automatically. (The timer is SPLIT: `timer` = presets, GENERIC/synced; `timerRun` = the running countdown, DEVICE_LOCAL — `timerAdopt()` takes presets ONLY so a peer's blob can never move a countdown onto this machine.) **Messages are split the same way:** `social`/`msgKey` = your identity (GENERIC/synced, so your @handle + keypair follow you across devices); `dms` = the message cache (DEVICE_LOCAL — it's a mirror of server data, re-pulled per device).

**Deck merge contract** (`mergeDecks`/`deckCanon`/`deckCap` in app.js, mirrored in webcache.js `wMergeDecks` AND store.py `merge_decks` — **three runtimes, one rule; a mismatch means phone/desktop/server settle on different decks**). The deck is no longer a whole document stamped with a client `Date.now()` (that let an open editor persist a stale copy over another device's question, let a fresh device's default deck steamroll a customized one, and let a fast clock win forever). Each ITEM merges independently:
- `updated` — the CONTENT stamp (excludes `ord`). **Set at the point of edit** by the editor, and **never re-derived inside the save** — deriving "what changed" from the stored copy is what let a stale editor array revert a remote edit and resurrect a remote delete.
- `ordAt` — the POSITION stamp, merged on its OWN clock, with **fractional `ord`** so a drag touches ONE item. (Renumbering the array on reorder would restamp every item and steamroll concurrent text edits — whole-document LWW sneaking back in through position.)
- `deleted` — a TOMBSTONE. Deleting KEEPS the item (hidden); removing it would let any device that still holds it re-add it on the next union.
- Winner per id: newer `updated` → exact tie: **tombstone wins** → still tied: **canonical-content STRING compare** (never a hash — a djb2 would have to agree across three runtimes' integer math).
- Anything byte-equal to a shipped default (DEFAULT_DECK, WIZ_SEEDS) stamps **`updated: 0`** — shipping code must never win a merge. Migration stamps a customization `1`, and **synthesizes tombstones for absent defaults** (deletion used to be expressed by ABSENCE, so without this the first sync resurrects every card the user ever deleted).
- **Two writers, never one:** `putDeck()` writes VERBATIM (all adoption paths — an adoption that restamps what it adopts makes two devices bump each other's stamps forever); `saveDeck()` is the user-edit path (merges, never stamps).
- **⚠️ BUILDING NEW DECK OBJECTS (routines / tasks / subtasks / habits / data fields)? These two rules are non-negotiable, and they are cheap now and expensive to retrofit:**
  1. **Every object gets a stable, globally-unique id at creation.** Per-item merge IS id-based. No id → it can't merge and falls back to whole-document clobbering. An id that changes on edit → every edit reads as a delete + a new object. An id that COLLIDES across devices → two different things silently FUSE into one and a user's data is destroyed. Mint ids like the deck editor does: base36 time + a device fingerprint (`devId()`) + entropy — never `Date.now()` + a small random, which two devices can collide on in the same millisecond.
  2. **Deletion is a TOMBSTONE, never an absence.** An absent object carries no information: a peer that still holds it cannot tell "you deleted this" from "I haven't seen your new one yet", so a union silently resurrects it. (This is exactly the trap the 2026-07-14 deck migration had to work around — it could reconstruct tombstones for missing DEFAULTS, but a deleted CUSTOM card was unrecoverable, because its absence recorded nothing.)
- Caps are applied to LIVE items and tombstones **separately** — one slice over a mixed array silently drops real questions or drops tombstones (deletes resurrect).

**Systems map** (added since the original CLAUDE.md — where the new stuff lives in `app.js`):
- *Character* — `cacheLevel()`/`renderCharacter()` (sidebar card), `logChar()` (journey arcs `JOURNEY` + skills + activity ledger), `getCacheName()` (founder → "King Cozy Cache"). **Edit profile** — `openProfile()` (full-screen `.daily-space` surface with dialog semantics — focus moved in, Tab trapped, restored to the opener, Escape defers to a stacked `.cat-modal`; `openCharLog()` is now an alias to it): the character view + inline autosaving edits (debounced saves are FLUSHED on close, never dropped). ALL editable text — name/role/note/pronouns/bio — lives on `money.profileCard` via `profileField()`/`profileName()` (legacy `money.profile` text reads through until the first card edit; `money.profile` is NEVER written by the surface — its text fields are exp-richer-wins and would revert edits, and its EXP witness must stay untouched; `setProfile` is a legacy no-caller). The public section's one opt-in (display name → the `profiles` row's `name`, `socialSetPublicName`/`profilePublicPayload`) treats the SERVER row as the authority: open heals the local flag toward it, a debounced text edit may only UPDATE an already-shared name (`requireShared` → `e.notShared`), and only an explicit toggle press can take a name from unshared → shared. "Switch account" routes to `openAccountMenu` when it exists (the logout-worktree's 2-tap menu), else cloud Settings — never a second switcher. Tests: `tests/profilecard_test.js`.
- *Cache health* — `cacheHealth()`/`renderHealth()`/`openHealth()`; at 100% `_healthFull` → `body.blessed` (gold cursor + `expSpark(...,true)` + `playShing()` from `av assets/shing.wav`); gives +10% EXP in `addExp()`.
- *Trust badge* — `renderTrust()` ← `/api/integrity` ← `store.verify_ledger()` (fsync + integrity checks).
- *Settings tiers* — `menuTier()`/`applyTier()`: `data-tier="N"` hides below tier; `data-menutier` on `<html>` restyles Minimalist(1)/Standard(2)/Legendary(3). (Renamed 2026-07-12; code + CSS + WIKI rename ships as one clean pass — see the rename item in BACKLOG.)
- *Fonts* — `applyFont()`/`FONTS` → `--font-ui` (loads Google font on demand).
- *Founder* — `isFounder()` (name = Cozy K Ace, or `money.founder`), `openKingCozy()` (retro console), `FOUNDER_COMPLIMENTS` vs public `PUBLIC_JOKES`.
- *Favorites* — `favs()`/`toggleFav()`/`autoPinOn()` (star → pin to top of library/dock).
- *Calendar* — a full-screen SURFACE (not a 13th area), its own `📅 calBtn` launcher by the action button (never touches it). `openCalendar()` (month/week/day + settings gear), `calThingsOnDay()` (the day-join: dated tasks + events + routine occurrences), `calMonthGrid()`/`calWeekDays()`, and the recurrence SPINE `calOccurrencesInRange()`/`calDaysInRange()`/`ymdOf()` that drive the pure `routineDueOn()` across a range. Events are a Thing `type:"event"` on `money.things` (start/end/allDay/startTime/endTime/sched) — `calAddEvent()`/`openEventDetail()`; they merge/sync for free (never store.py). Prefs in `money.calview`.
- *Messages* (Community area #7 · P5 social layer) — a full-screen SURFACE with its OWN `💬 msgBtn` launcher, pinned FAR-LEFT of the deck+calendar trio (`applyDockConfig`; unhideable like them). `openMessages()` is one surface with views: onboarding (connect cloud / claim @handle) · conversations list · thread + composer · find-friends · requests. E2E DMs **between accounts**: found by a public **@username** in a new PocketBase `profiles` collection; a per-account **ECDH P-256** keypair (`socialEnsureKeypair`) seals bodies with AES-GCM under the pair's shared secret (`socialSeal`/`socialUnseal`) so the server stores **ciphertext only**. `socialClaimUsername`/`socialSearch`/`socialRequest`/`socialAccept`/`socialSend`/`socialPoll` all hit PocketBase directly (like `cloudPush`) — **no server.py/store.py change**. **Backend prerequisite (Cozy creates in the pockethost admin):** three collections — `profiles` {owner,username,pubkey,name}, `friendships` {from,to,status}, `messages` {from,to,body,epub}. Every call try/caught so a not-yet-created collection just shows a calm empty state. Opt-in (undiscoverable until you claim a handle); lone-wolf stays 100% local. Poll piggybacks the 75s cloud loop + 15s while the surface is open. See `tests/social_test.js` for the crypto + key-class guarantees. **The notification center** lives inside this surface as the "What's new" view (🔔 in the header, reachable logged-OUT too — the Cache's own news needs no account): notifications come from **`release-notes.json`** at the repo root — a static array `[{id, date, title, body}]`, newest first, copied by `build-app.sh`/`build-demo.sh` so it deploys in LOCKSTEP with the code it describes (⚠️ **add an entry per user-facing release**, ids like `YYYY-MM-DD-slug`; every field renders through `escapeHtml`). `#msgBtn` badges TWO corners: top-right `.msg-unread` = people, bottom-right `.msg-news` = news (different shape/tone on purpose). Read state auto-marks after ~5s of GENUINE visibility (IntersectionObserver + visibilityState — a backgrounded tab never marks anything read) and is reversible via a visible ⋯ menu on every row (also right-click / context-menu key / long-press — never gesture-only). First-ever run seeds the whole file as read (zero fabricated notifications); see the `money.notifs` entry above for the merge class.

**Where context lives** (so a reset is safe): `Working Docs/Brain Bucket/` = Cozy's actively-held working memory — he hand-drops ANY file he wants every session to hold in its head; read all of it at session start, gitignored, never commit it. When Cozy says **"clean out the brain bucket"**, walk each file with him one at a time — keep (still active) / file it where it belongs / toss — and since the device bridge can't delete, "toss" means move it into `Working Docs/Brain Bucket/_toss/` for him to empty. A monthly scheduled reminder (15th) nudges the cleanout so the bucket stays working memory, never a junk drawer. The five internal planning docs live in `Working Docs/` at the repo root, numerically prefixed by information hierarchy. `Working Docs/1_PRINCIPLES.md` = the WHY (evidence-based design rules for cognitive accessibility). `Working Docs/2_STRUCTURE.md` = the WHAT (12 life areas + 6 shared systems + area contract + main snapshot shape). `Working Docs/3_ROADMAP.md` = the WHEN (written 2026-07-12: the vision chain, the EF-energy north-star metric, Now/Next/Later lanes, decisions log — the check-in reads its NOW lane first). `Working Docs/4_CLOUD-PLAN.md` = cloud architecture specifics that support STRUCTURE. `Working Docs/5_BRAND.md` = private design references. `Working Docs/6_PROJECT-MANAGER.md` = operational standup protocol. **`Working Docs/7_STRATEGY.md` (PRIVATE, gitignored) = business, legal, licensing, monetization, governance and security posture.** Read it BEFORE doing any work that touches those areas — and never quote, summarize, or paraphrase its contents into a public file (this one included). At the repo root: `BACKLOG.md` = every ask (shipped `[x]`, in-progress `[~]`, open `[ ]`) + the public roadmap. `FEATURES.md` = shipped product list (currently frames Cache as a money app; scheduled for life-OS rewrite). Agent **memory** holds the earlier north-star: `money-vision` (cache-as-character, Community Cache + wealth-redistribution, King Cozy = primary user), `money-cockpit-project`, `data-safety`, `user-profile` — the life-OS reframe subsumes this.

## Workflow

- **Cache check-in (project manager).** When Cozy says **"cache check-in"**, "standup", "where are we", or "get me back on task" — and when the scheduled `cache-standup` task runs — follow **`Working Docs/6_PROJECT-MANAGER.md`**: a fast status snapshot, the 1-3 next moves, dropped threads, a north-star gut-check, and always **one big baby-step action** to close. It's the antidote to a 150-item backlog. Edit `PROJECT-MANAGER.md` to change how it behaves.
- **I can't see localhost** (sandbox can't reach the user's loopback). The user is the eyes — hand back and ask them to look; for risky visuals, preview with the visualize tool first. Don't ship intricate generative SVG blind.
- **The backend doesn't hot-reload.** After editing `*.py`, the user must restart `python3 server.py`. Static files (`*.js/.css/.html`) just need a browser reload.
- **Verify before handing back:** `node --check app.js`; `python3 -c "import store"` (and run a quick logic check that prints counts/placeholders, never real data).
- **⚠️ `./tests/run.sh` — RUN IT BEFORE AND AFTER ANY SYNC-ENGINE CHANGE.** 147 assertions, plain node+python, no deps. It covers the per-key localStorage merge, the authored-hash witness, the backend map/catmeta/tombstone merges, the merged restore, the `rev` stamp, the timer split, and the deck's per-item merge — including a 3-runtime parity fixture proving app.js, webcache.js and store.py pick byte-identical winners. **Adversarial review found a livelock or a data-loss bug in EVERY draft of this engine; these assertions are what pinned them down.** Harnesses load the real functions by ANCHOR (function-name regex), never line numbers, so they don't rot. If you add a `money.*` key or a merged data file, add a scenario. Fixtures are placeholders only — never real bank data.
- Run: `python3 server.py` → open `http://localhost:5173`.
- **Keep `BACKLOG.md` current.** Append any request or idea the user mentions (even in passing) as a `- [ ]` item; check items off `- [x]` with the date when shipped; proactively surface relevant open items. The user relies on this so nothing they ask for gets dropped — don't stop-and-ask one at a time and forget the rest.
- **Keep `FEATURES.md` current.** When a user-facing feature ships, add it in product language (benefit-framed) — it's the launch/marketing list for when Money opens to other people. Both files are surfaced in-app via the Roadmap pill (Roadmap + Features tabs).
- **Commit SUBJECTS are the in-app changelog.** The "Update app" preview (`/api/update-check`) shows each pending commit's subject line to the user before they pull. So write the **first line** of every commit benefit-framed and plain-English ("Forecast: real-effort overlay vs projection", not "fix fn refactor") — it's what a friend reads to decide whether the update is worth it. Keep the noisy details in the body.
- **⚠️ The roadmap is PUBLIC.** `BACKLOG.md` and `FEATURES.md` render live on the public website: `docs/roadmap/index.html`, published via GitHub Pages (`cozykace/thecache`, source = `main` `/docs`, custom domain **https://thecache.app** — DNS at Squarespace points straight at Pages, HTTPS enforced). It fetches the raw `.md` files on every load, so anything written there goes live the moment it's pushed. Write backlog/features in language that's safe for anyone to read — no private notes, no real data, no half-baked internal asides.
- **The site map (2026-07-13):** `https://thecache.app/` = the hosted web app (login gate; built by `build-app.sh` into `docs/` root — run it after any `app.js`/`styles.css`/`webcache.js`/`index.html` change) · `/roadmap/` = the public roadmap · `/demo/` = the public demo · `/status/` = the public status page (`docs/status/` — live browser-side health checks + `incidents.json` history; log every user-facing incident there as `{date,title,status:investigating|monitoring|fixed,updates[]}`; the report box POSTs `kind:"outage"` to the feedback collection with a mailto fallback) · `/app/` = a redirect stub to `/` (old links + home-screen icons). Old `cozykace.github.io/thecache/*` URLs redirect to the domain.
- **Public demo** (`docs/demo/`, served at `https://thecache.app/demo/`). It runs the **real app** — `app.js`/`styles.css` are copied in by `build-demo.sh`, and `docs/demo/demo-data.js` (loaded before `app.js`) seeds a curated board + intercepts every `/api/*` and `data/*.json` fetch with **play numbers only**. No backend, no real data ever. **Run `./build-demo.sh` after any `app.js`/`styles.css`/logo change so the demo doesn't drift**, then push (`main` → live). If you add/rename a backend endpoint or change an API response shape, update the matching route + fake payload in `demo-data.js` (route order matters — more specific paths first, e.g. `income-links` before `income`). I can't render JS here, so the user eyeballs the live URL and we iterate.
