# THE CACHE — backlog

What's coming, what's in progress, and what's recently shipped in THE CACHE. New ideas get added as they come up and checked off as they land.

_Last updated: 2026-06-25_

## The accuracy engine (fix-in-UI, no AI; all ripples via the Store)
- [x] **Category Manager** (Menu → Manage categories) — lists every category + # of transactions; rename (server-stored label, ripples to every widget); delete → fold/merge into another category (batch); click a category → recategorize its merchants one-by-one; + new category. Backend: catmeta.json registry (labels+remap), categorize() applies remap (2026-06-22)
- [x] **Recurrence detection** — detect_recurring() scans the whole ledger (all accounts incl. cards) for ~monthly clustered charges; Subscriptions widget now shows a "detected · not tracked" section with one-click "+ track" (found 16 untracked recurring) (2026-06-22)
- [x] **Review inbox** (status pill → Review panel) — triage: possible duplicates (remove one), recurring-not-tracked (+ track), uncategorized (inline category select), untagged deposits (tag); every fix ripples via the Store. Pill updates cheaply from Store data; full list fetched on open. Added /api/issues, /api/categories, /api/recurring, /api/category, /api/delete-txn (2026-06-22)
- [x] **Toggl work widget + auto-pull** — toggl_sync.py pulls time entries (token in gitignored .toggl), correct running-timer math; "Time worked" widget shows today/week/month hours + $ at your rate + by-project + live timer; LaunchAgent com.thecache.togglsync auto-pulls 4×/day + login. No AI (2026-06-22)
- [ ] **Connect/CSV all cards** — connected cards already capture real purchases; for cards without a SimpleFIN connection the only fix for purchase visibility is connect-or-CSV (detail isn't in the checking batch payment)
- [x] **Auto-sync** — LaunchAgent `com.thecache.sync` runs sync.py at 8am/2pm/8pm + on login; data stays fresh with no clicking or AI. Sources panel shows "auto-syncs 3×/day". Logs to data/sync.log (2026-06-22)

## Features
- [ ] **🧩 Form builder — build, save, and route your own data-intake forms** (BIG) — a way to design form templates (label + field types: text, number $, date, choice, yes/no, notes), **save them as reusable templates**, browse + favorite them, then fill a form to **push the data into the cache** — with you choosing **where it lands** (which data file / category / a new custom store). Think "the tagger system, but you build the intake side too." Foundation for the family life-planning use case below. Bricks: (1) a Forms widget/library (list + favorites), (2) a template designer (add/reorder fields), (3) local storage of templates + submissions (gitignored `data/forms.json` + `data/form-data/*`), (4) a routing step (where each field's value is stored), (5) backend save endpoint. Requested 2026-06-26.
- [ ] **👨‍👩‍👧‍👦 Family asset — multi-person life planning on one shared computer** (BIG, north-star) — make the cache usable by a whole family sharing one machine: lightweight **profiles** (switch who's using it), shared vs. private data, and life-planning intake via the Form builder. First step: a short **feedback form to send brother Marc + family** (big family, lots of kids) so they can test and tell us what would make it a life-planning asset they'd share. Ties to the Community Cache + minigames vision. Needs: profile switching, per-profile data scoping, and the form builder. Requested 2026-06-26.
- [ ] **☁️ The Cache cloud — opt-in, protected hosted cache** (BIG, north-star) — the long-term goal: anyone can **choose** to run their cache on a hosted Cache cloud server (so it works on iPhone/iPad and any browser, no computer needed) — and we **protect it like a vault**: encryption in transit + at rest, per-user auth, no selling/snooping, the same privacy ethos as the local app but server-side. Local-first stays the default; cloud is an opt-in. This is the backend that also unlocks DMs/chat, family multi-profile, and community minigames. **Starting point (now):** Spencer (and testers with a computer) run it **locally on a desktop Mac** — that path works today; iPhone-only support waits for the cloud. Needs: hosting, auth, encrypted per-user data, a sync layer. Requested 2026-06-26.
- [x] **Crowd applause on trip landing** (2026-06-26) — booking a trip to the cache now lands with a synthesized crowd cheer/applause (WebAudio, no asset) the moment you arrive; skipped on the calm/reduced-motion path and when the trip music is muted. Requested + shipped 2026-06-26.
- [ ] **💬 DMs + find friends — a chat pane inside the cache** (BIG, community) — a direct-message panel so you can chat with people you know (e.g. Cozy ↔ Spencer) and a place to find + meet other cache friends. The social spine that the minigames + Community Cache hang off. Needs the community/membership backend + identities (the app is local-only today, so this needs a real server + auth + presence). Requested 2026-06-26.
- [ ] **🗺️ Miro-style infinite workspace** — make the board feel like Miro: a big/near-infinite canvas with smooth pan + zoom, easy navigation (fit-to-content, minimap, jump-to-widget), and room to spread out far more than today's fixed 3200×2200. Build on the existing zoom/pan + saved views. Requested 2026-06-26.
- [x] **Cartridge clunk-in** (2026-06-26) — replaced the scroll "time machine" with a one-shot load animation: the cartridge stats bar drops and seats into the gold King slot, which gets depressed by the impact and springs back up to rest. Respects reduced motion. Requested + shipped 2026-06-26.
- [ ] **🎮 Community-only minigames — juice your character, then play others** — the ONLY minigames in the cache are community/multiplayer: do the real money work to level + equip your unique cache character, then face other caches (family kids, other users) with your juiced-up character. Single-player stays 100% real-life; games are the social reward layer. Needs: the community/membership backend + character stats as game inputs. Requested 2026-06-26.
- [x] **Stats bar = game cartridge → plugs into the gold King slot** (2026-06-26) — the personal stats bar is now styled as a game cartridge (solid beveled body, sticker-label line, gold edge-connector fingers on a green-tinted strip — RAM/Sega vibe) for every profile. For the founder, the King Cozy bar became a gold cartoon-manga slot (cel-shaded gold body, dark outline, slot mouth, vines curling around the ends) that the cartridge plugs ~⅓ into; scrolling the board seats the cartridge a little deeper. NEXT: tune the plug depth / connector look; optional neutral slot for non-founders.
- [x] **Income forecast sliders bank EXP** (2026-06-26) — dragging an income-forecast slider now earns cache EXP for the effort (~1 EXP per 50px the thumb travels), feeding your character. Requested + shipped 2026-06-26.
- [x] **Mono default — purer white + Light/Auto/Dark tier toggle** (2026-06-26) — the default mono light scheme is now clean pure white (no panel tint, no dotted texture), and the palette menu gained a higher-level **Light / Auto / Dark** tier as chunky 3D analog buttons that physically depress when engaged (Auto follows your system). Color profiles sit below it. Requested + shipped 2026-06-26.
- [~] **🌌 Enter the Ledger — skull CTA → lightspeed warp → a different space** (FLAGSHIP) — **v1 SHIPPED 2026-06-26** (prototyped, then built): the 💀 pill triggers a full-screen starfield hyperspace warp → white flash → **THE LEDGER**, a cosmic space showing your cache total + your months as a glowing constellation (gold=net+, ember=net−) from real monthly.json; Esc/Return exits. **v2 (2026-06-26):** reframed as an RPG "book a trip to visit your cache in person" (booking screen → warp → arrive); approved soundtrack `slingshot the airplane v4` (starts/loops from the 3s mark, mute button); synthesized warp.wav whoosh; every trip banks +25 EXP with a count-up + star reward; colors pull the live `--accent`. **In-cache UI started:** spaceship-cockpit dashboard bubbles along the bottom (first = Data Sources, color-coded via the new monday.com-style `LG_COLORS` design language), the back button is now **Return**, stars settle toward a halt after arrival, and click-drag orbits the field in 3D. All warp/flash/spin gated behind `reduceMotion()`. NEXT: more cockpit bubbles (cash/spend/months/EXP) + tie their colors into the constellation; click a star → that month's story; fly through months / accounts-as-planets; warp tuning.
- [~] **Accessibility panel + system** (started 2026-06-26) — Menu → ♿ Accessibility. Built as a small system: each need persists in localStorage → applied as an attribute on `<html>` (`data-reduce-motion` / `data-contrast` / `data-text`) → CSS + JS read it. Adding a need = one key + one attribute + one CSS rule + one panel row. **Shipped v1:** (1) **Motion & flashing** — auto (follows OS `prefers-reduced-motion`) / reduce / full; *reduce* = seizure-safe path: warp does a calm fade (no fast star streaks, no white flash, no whoosh), all CSS animation/transition neutralized, EXP sparks + constellation twinkle suppressed via a single `reduceMotion()` gate. (2) **Contrast** high — stronger border vars + text. (3) **Text & UI size** — `zoom` 1.12 / 1.26. Plus app-wide `:focus-visible` outlines. **Hub upgrade (2026-06-26):** the panel is now the **♿ Accessibility Hub** — welcoming, creative, game-flavored (not a sterile dialog): one-click **comfort presets** (🌙 Calm / 🔆 Crisp / 🚀 Full FX) that bundle the fine-tune settings + live highlight when matched, a warm "built for how *you* use it" intro, and a footer that says accessibility requests jump the line. **STRATEGIC:** a11y is a core differentiator + monetization angle — target disability-focused power users / gamers (~$10/mo); messaging added to the wiki Accessibility Hub section + Developer Center ("why the build"). See memory `accessibility-positioning`. **Keyboard + screen-reader pass (2026-06-26):** every `.cat-modal` auto-becomes a focus-trapped `role="dialog"` (aria-modal + aria-label from its title; Tab cycles inside, Escape closes, focus restores to the trigger on close) via a global MutationObserver — zero per-modal wiring; hub segmented + preset buttons expose `aria-pressed`; a "Skip to dashboard" skip-link, `role="main"` on the board, `role="navigation"` on the sidebar. **Color-blind-safe (2026-06-26):** Accessibility → Color vision = Safe swaps the visualizer to the Okabe-Ito palette (constellation pos=#0072B2 blue / neg=#D55E00 vermillion; cockpit bubbles via `lgPalette()`), and +/− signs mean color is never the only signal (`data-cb` attr). NEXT: reduce-transparency, dyslexia-friendly font, bigger touch targets, `prefers-contrast`, full SR sweep of dock/widgets, aria-live for sync/save status. Requested 2026-06-26.
- [ ] **Gutter slider — Apple-style, 4 detents** — make the gutter bar feel like an Apple control: ~4 sections it softly snaps to (magnetic, but you can still land in between), tick marks, "less ←→ more" labels. Keep the resize-in-place behavior. Requested 2026-06-26.
- [x] **Tidy layout, in place** (2026-06-26) — keeps widgets in their existing rows + order, aligns tops, evens gaps, and normalizes widths to fit the canvas (was: reflowed everything to reading order / "flew to random places").
- [x] **Your cache is a character** (2026-06-26) — name your cache whatever you want ("<Name>'s Cache" by default), and it levels up as you do the work: level + title + XP bar at the top of the menu. EXP auto-earns from real activity — no shop, nothing to buy.
- [ ] **Cache RPG depth** — multiple named caches per login, a combined "profile character" merging all your caches (for supporters / free verified testers), and widget / feature / art unlocks earned purely by progress (never purchased). Long-term north-star.
- [ ] **Cursor magnification (Mac-dock style) — unlockable character skill** — UI near the mouse subtly magnifies, like the macOS Dock. Feasible for DISCRETE items (scale widgets / dock items by cursor distance — clean + performant); a true continuous "fisheye lens" over arbitrary content is much harder (DOM can't be magnified like an image — needs canvas/SVG re-render, heavy). Plan: ship the discrete-item version as the FIRST earned character-skill UNLOCK (ties to the journey). Requested 2026-06-26.
- [x] **Font packs** (2026-06-26) — pick the whole-UI typeface in Settings → Fonts (Terminal · Hacker · Space Cadet · Clean Slate · Editorial · Typewriter · Brutalist); each previews in its own font, loads on demand.
- [ ] **Music as a hidden gem** — the Soundtrack/music dock feature is hidden by default; you *discover* it by switching to **Galaxy Brain** (max mode). Once you favorite it (or any feature), it's exempt from tier-hiding and stays put even if you drop back down. Requested 2026-06-26.
- [ ] **Rad art backgrounds (+ custom)** — the whole app shows beautiful art backgrounds you can pick from, plus add your own. The current Backgrounds picker (✨ top-right) doesn't really work yet — fix + flesh it out. Ties to the design-aesthetic work. Requested 2026-06-26.
- [ ] **Cache character ledger** — give the character its own append-only ledger of clicks, EXP gained, and notable events (what happened + when), so character-building is as rock-solid + auditable as the money ledger; derive level/stats from it. Requested 2026-06-26.
- [ ] **Cache character journey (feats + earned unlocks)** — progression driven by REAL actions, not click-grinding: arcs Awakening→Foundation→Momentum→Mastery→Legend (Lvl 1–10), each with feats (First Sync, Income Mapped, Budget Built, On Time, Streak Keeper, Verified Vault, …). Feats/levels unlock widgets, themes, art backgrounds, features — earned, never bought (no shop). Powered by the character ledger. First draft 2026-06-26.
- [x] **Click sparks + trust badge + demo opt-in** (2026-06-26) — rapid clicks shoot theme-colored EXP sparks (5→10 thick); a live "data verified ✓" trust badge in the menu pane (per-check breakdown from /api/integrity); the public demo can be wrapped in a click-to-load poster so it doesn't auto-run on mobile.
- [ ] **Fully-offline option (vendor CDN assets)** — self-host fonts / Lucide / Motion and drop the YouTube embed so the browser makes ZERO external requests — airtight privacy + works with no internet. Requested 2026-06-26.
- [ ] **Develop a specific design aesthetic** — use Claude's design tooling to define a stronger visual identity beyond the current themes (type scale, spacing system, component styling, motion, brand voice). Requested 2026-06-26.
- [ ] **Category budgets — goal vs actual, week/month toggle** — set a spending target per category; a view lists every category with its budget vs actual spend (progress bar, over/under), toggleable between a **monthly** and a **weekly** window (weekly target = monthly ÷ 4.33, or set its own). Builds on the period logic + category data (coreflex/breakdown). Requested 2026-06-26.
- [x] **Income forecast → stacked streams view** (2026-06-26) — stacked income-by-source over time: real per-month history from the ledger (`/api/income-monthly`) → flat slider projection, "needed" line, "now" divider, per-source legend toggles + history-link, and a `streams | cushion` mode switch. Plus a real-effort overlay: Toggl hours × gig rate (`/api/work-monthly`, `toggl_sync` banks monthly hours) drawn over history → projection.
- [x] **"The Cache" brand theme** — a warm-dark color scheme with the brand gold (#FFD409) accent, now leading the theme picker; the masked wordmark logo tints to each theme's accent (gold on the brand theme) (2026-06-25)
- [x] **Custom stat trackers** in the stats bar — built-in streak ("Expenses streak": mark each month paid-on-time → "🔥 N mo" consecutive) plus user-built trackers: monthly streak, days-since-a-date, count of bank purchases matching a word (/api/match-count), or a manual +/- counter. Managed in Settings → Stats bar; toggle/drag into the top bar like the built-ins (2026-06-25)
- [x] Link income sources to Toggl projects — Money Map "money in" rows have a "→ link work" picker (your Toggl projects); once linked, the row shows that project's monthly hours so income lines up with the work that earns it. Durable in income_links.json (2026-06-23)
- [x] Money-flow node map — "Money flow" widget: SVG account cascade (checking→savings→cards), credit-card toggle, income/spend + recurring-transfer bubbles (/api/transfers) (2026-06-23)
- [x] Magnet snap for stickers / icon-nodes — stickers snap to the 24px grid by default; hover magnet toggle to free-place (accent when on), reuses the widget snap machinery (2026-06-23)

## Sharing / let a friend try it
- [x] In-app **Connect a bank** coaching panel (Menu → ⚡) — step-by-step SimpleFIN setup + paste-your-token field → `/api/connect` claims it inline (server-safe, no CLI sys.exit) + first sync; **Load demo data** (real SimpleFIN demo claim URL) + **Import CSV** free paths; demo guarded by confirm so it can't clobber a live connection. Per-user (each person's own token, stays on their Mac) — no enterprise/aggregator plan, no shared tokens (2026-06-25)
- [x] In-app **Update app** button (Menu → ⟳) — `/api/update` does `git pull --ff-only origin main` then restarts; works for anyone running a clone, so pushed updates land with one click (2026-06-25)
- [x] Friend onboarding guide — `SETUP.md` (clone → start.command → demo data or own bank → Update app); each person runs their OWN local copy, data stays on their machine (2026-06-25)
- [x] Fixed local git corruption — stray macOS dup refs `refs/heads/main 2` + `refs/remotes/origin/main 2` (zero-SHA) broke every fetch/pull; removed (2026-06-25)
- [x] Repo made PUBLIC (2026-06-25) — verified zero data/secrets ever committed (full-history scan); code-only, all data gitignored. Friend can clone + Update with no auth
- [x] **Privacy pass on public code** (2026-06-25) — genericized real client/gig names out of app.js + the public demo: forecast defaults now read "Monthly retainer" / "Gig work", help/labels say "gig work" instead of a named platform, demo uses fictional sources. Backend still auto-detects gig/payroll deposits. Re-verified no client names, tokens, or data anywhere in tracked files or git history
- [x] In-app **Report a bug or request** (Menu → ⚑) — type picker (bug / request / other), message + optional reply-email, sends straight to cozy@cozyace.com with app context; silent auto-send via Web3Forms (key set 2026-06-25), no mail app needed (2026-06-25)
- [x] **Public live roadmap** — BACKLOG/FEATURES render live at cozykace.github.io/thecache, embedded in the Squarespace site at thecache.app; updates the moment a change is pushed (2026-06-25)
- [x] **Public demo — runs the real app on fake data** (docs/demo/ → cozykace.github.io/thecache/demo/) — boots the actual app.js/styles.css with a demo-data.js layer that seeds a curated board + answers every backend call with play numbers; fully interactive, always matches the app, no Python backend and no real data. `build-demo.sh` keeps it in sync. Replaces the old static mock (2026-06-25)
- [x] Web3Forms key set — bug/request reports now auto-deliver to cozy@cozyace.com with no mail app needed; the public demo's button is stubbed so it can't be a spam vector (2026-06-25)
- [ ] Optional: an `install.command` that clones + sets up the auto-start LaunchAgent for a non-technical friend
- [x] **Trackable downloads via GitHub Release** (2026-06-26) — shipped **v0.1.0** as a tagged GitHub Release with `thecache-v0.1.0.zip` as a real download asset (release assets ARE counted; branch zips aren't). New `/api/downloads` sums asset `download_count` across releases → live **downloads** stat on the King Cozy bar + console, and (founder machine only) logs `cache_download_total` to PostHog whenever it changes, so downloads chart over time. Last count cached in `data/downloads.json`.
- [ ] **Shipping path: Spencer → anyone → help wiki** — (1) polish until it's shippable for friend **Spencer** to try; (2) make it so **anyone can try it with zero help** (frictionless install + onboarding); (3) build a **help wiki** (likely both a Squarespace help page on thecache.app AND a GitHub wiki/`/docs`). Requested 2026-06-26.
- [ ] **Prettier local address** — replace "localhost:5173" in the browser via a custom local hostname (e.g. a `/etc/hosts` entry `127.0.0.1 cache.local` → http://cache.local:5173), and/or just nail the tab (goat favicon ✓ + good title). The address bar can't be fully styled, but the hostname can be friendly. Requested 2026-06-26.
- [ ] **Download button on thecache.app** — a clear "get THE CACHE" path: repo ZIP (github.com/cozykace/thecache/archive/refs/heads/main.zip) + SETUP.md, or a one-click installer. Requested 2026-06-26.
- [~] **Protect the art — ship builds, not source** (DECIDED 2026-06-26) — the visualizer + look-and-feel are Brent's IP. ✅ Proprietary `LICENSE` added (source-visible for trust, but no copying/redistribution/derivatives/commercial use; personal local run is fine). ✅ Developer Center started in `WIKI.md` (teaches the *approach*, not the art). **NEXT, at ship time:** move readable source to a PRIVATE repo; produce an **obfuscated/minified build** for the public demo + downloadable releases; auto-update pulls a release (not the source). Do this when closer to shipping (test the obfuscated build carefully — can't verify rendering here). Note: the current public repo's git history still contains readable source, so the private-source cutover means a fresh public "builds-only" repo. (Possibly what **dfos** is for — clarify.)

## Mobile
- [x] Mobile-friendly SOPs added to the process stack (CLAUDE.md → "Mobile-friendly (SOP)") — touch targets, no hover-only, fluid/container-query sizing, touch-action, sheets-under-480px, safe areas (2026-06-25)
- [x] Mobile stacked layout — under 640px the freeform board becomes one vertical scroll of full-width widgets; drag/resize/zoom disabled so a finger scrolls (board stays freeform on desktop). Chart widgets get explicit heights; stats bar/dock fit + scroll; safe-area insets respected (2026-06-25)
- [x] Hover-only audit — sticker controls/resize grips now show on coarse-pointer (touch) devices via `@media (hover:none)` (2026-06-25)
- [x] Remote access path — server takes `GOAT_HOST` (default localhost; opt-in LAN); Tailscale `serve` for private "anywhere" access (2026-06-25)
- [ ] Drag-to-arrange on touch — phones are view/scroll only; rearranging still desktop-only (an explicit arrange mode could add it later, probably unnecessary)
- [ ] Convert fixed desktop popovers (settings, period/clock menus, pickers) to bottom-sheets under ~480px
- [ ] Per-source slider: optional hourly-vs-monthly choice when adding (currently monthly default)

## Trust & durability (PRIORITY)
- [~] **Rock-solid data ledger — security + redundancy** (IN PROGRESS) — make the local ledger bulletproof. ✅ Shipped 2026-06-26: crash-durable writes (fsync on all three write paths + dir fsync) + a non-destructive integrity self-check (`verify_ledger` → `/api/integrity`: readable, unique ids, well-formed, no corrupt lines, recoverable backup). **Next:** surface a "data verified ✓" trust badge in-app; pre-write rollback snapshot before each merge; a *tested* restore (verify a backup actually restores); rotating + offsite-optional backups.
- [ ] **Industry-advancing trust ideas** (Brent: "push the industry forward, for real") — (1) **tamper-evident hash-chained ledger** — each appended line carries a hash of the previous, so history is cryptographically verifiable/append-only (you can PROVE nothing was altered); (2) **encryption at rest** for `data/` from a passphrase, so the files are unreadable if copied; (3) **signed, portable export** — take your whole cache as an owned, verifiable bundle (true data sovereignty); (4) deterministic recompute = every number provably derived from the raw ledger (already the design — make it auditable/visible). Requested 2026-06-26.

## Install & reach
- [ ] **Easy cross-platform install (PC + Mac), browser-run, in-app updates** — today it's Mac-centric (`start.command`, LaunchAgents, `~/thecache`). Add: a Windows launcher (`start.bat`) + Windows auto-start (Task Scheduler/startup shortcut), a dead-simple installer for non-tech users, keep the browser UX + the existing in-app **Update** button. (Spencer mentioned **DFOS** / dfos.com — clarify what it does + whether it fits.) Requested 2026-06-26.
- [ ] **Headless-AI-home-server positioning** (Brent's insight) — local-stored + browser-accessed is *ideal* for someone running a headless home AI server: the AI can manage a person's real life through THE CACHE locally, private by default. Frame/build toward this (clean local API surface, AI-operable, no cloud dependency). Long-term but informs architecture. Requested 2026-06-26.

## Community (opt-in; lone-wolf stays 100% local)
- [ ] **Architecture: local-secure data + code updates + online friend connections / co-caches** — keep each person's full ledger LOCAL + private, while enabling (1) code updates [already solved: git-pull Update button — code ≠ data], (2) online friend links + **co-caches** (shared spaces friends pool/compare progress in). Design principle: **the server never sees raw financial data** — co-cache data is **end-to-end encrypted** client-side (WebCrypto/libsodium), server stores only ciphertext (zero-knowledge); nothing syncs unless explicitly shared; content-addressed + signed so tampering is detectable (ties to the hash-chain). Roadmap: auth/accounts → E2E shared-space primitive → co-cache features (shared goals, contribution ledger) → all opt-in. Requested 2026-06-26.
- [ ] **🎁 Gift drop — surprise-release a finished feature to all members on demand** (founder) — hold finished "gift" widgets behind feature flags (code is shipped but gated OFF); a founder button randomly picks one unreleased gift and "drops" it: clients fetch a remote gifts config (`docs/gifts.json` via Pages, polled on load + on Update) and enable it — relatively instant for everyone, no per-user push needed. Needs: a gifts manifest + flag-gated widgets + the remote config + the founder drop button. Pairs with King Cozy founder stats + the membership backend. Requested 2026-06-26.
- [x] **Anonymous, opt-in usage analytics — PostHog** (2026-06-26) — wired with the project key + US host. **Opt-in** (Settings → Share anonymous usage, default OFF); **autocapture + session recording OFF** so it can never scoop dollar amounts/merchants; captures only named safe events (app_loaded w/ widget list, widget_added, rage_click [element id/class only], client_error). Anonymous, respects DNT. NEXT: add more events as needed (period changes, feature opens) + a PostHog dashboard. so we see which widgets get used most, click heatmaps, rage-clicks (reuse the click-streak detector), and JS errors (what's probably broken). IRONCLAD rules: opt-in (default off), anonymous (random install id, no PII), and **financial data NEVER leaves** — only interaction metadata. Routes: (1) **PostHog** [autocapture + heatmaps + rage-click + error tracking, free tier, self-hostable] — fastest/richest; (2) custom telemetry → Supabase [100% owned, more build]. Needs founder's backend choice + an opt-in toggle + event instrumentation. Requested 2026-06-26.
- [ ] **King Cozy founder stats** — a founder-only subsection (gated to Cozy K Ace) aggregating: features shipped, bugs squashed, total commits, app downloads/clones, testers, EXP across the project, etc. Locally-computable ones (commits, features) are easy; downloads/clones/stars need the GitHub traffic API (token + network). Requested 2026-06-26.
- [ ] **Community Cache Ledger (= the company ledger)** — one transparent, honest ledger of every member's effort + value contributed (the personal-cache integrity values, at the community level). **The economic model (Brent, "this is important"):** members who opt in **offer their anonymous data connection to benefit the whole**, and get **actual CASH back from wealth-redistribution cycles** — so THE CACHE is also a way to *make cash* by managing your real-life resources. **Opt-in:** lone-wolf stays 100% local/offline forever; the community layer needs internet + accounts + the E2E backend, and the privacy promise must hold. Big — needs backend we don't have yet. Requested 2026-06-26.
- [ ] **Reviews collected in the Community Cache Ledger** — let members leave reviews that live in the community ledger (a contribution + social-proof signal, alongside effort/value). Needs the community backend. Requested 2026-06-26.
- [~] **King Cozy console** — founder retro-futuristic build dashboard shipped 2026-06-26 (features/fixes from BACKLOG, GitHub stars/forks, EXP, days). **King Cozy bar (2026-06-26):** the console's numbers now also live as a persistent founder-only SECOND stats layer stacked just beneath the personal stats bar (EXP, days building, shipped/in-progress/planned, fixes, stars/forks, live PostHog events & people) — click it to open the full console; the normal member's stats bar is untouched. **Machine-locked (2026-06-26):** all founder surfaces (bar + King menu + console) are now gated on a machine-local secret (a gitignored `.founder` file the backend checks) instead of the typed name — typing the founder name can no longer unlock anything, and the public demo (no backend) is always locked. **Time Machine layering (2026-06-26):** scrolling the board down makes the personal stats bar recede (shrink + blur + fade) behind the King Cozy bar like the macOS Time Machine depth stack — founder-only, respects reduced motion. **Secret stats window (2026-06-26):** the King console gained a "🔒 deeper · tucked away" section — member №1, cache level/title, interactions, plus a founder-gated `/api/king-stats` feeding commits, lines of code, files, ledger txns tracked, accounts, and days covered. NEXT: make it an **unlockable achievement** (gate it behind a milestone instead of just the founder-name check); wire real downloads (via Release) + commits (GitHub API).
- [ ] **Cozy-only stat: community members connected** — a founder-gated number showing how many users are connected to the Community Cache. Needs the membership backend. Requested 2026-06-26.
- [ ] **Select-tester update targeting** — push a test/real update to ALL or *chosen* testers; depends on the membership backend above. For now `tester-bump.sh` bumps everyone on `main`. Requested 2026-06-26.
- [ ] **Widget reliability pass** — every widget rock-solid: no stale/empty states, graceful when the backend is mid-restart, consistent numbers everywhere (already one-Store, but re-verify). Requested 2026-06-26.

## Smarter / more robust
- [x] **Minimal Mono default — calm black-and-white that follows your system** (2026-06-26) — the new default look is a clean greyscale theme that auto-inverts with your computer's light/dark setting (grey-black on off-white by day, off-white on near-black at night). Accent collapses to ink so the UI reads genuinely black-and-white, and the dotted paper texture is dropped for a calmer, less-noisy canvas. All the color themes (The Cache, Oat Milk, Goblin Mode, …) are still one tap away in the palette menu. Goal: a simpler, easier-to-read visual language by default.
- [x] **Time Machine bar layering — refined** (2026-06-26) — gentler depth (less shrink/blur/fade) and the two stat bars now **converge to overlap** as you scroll, landing stacked with the King Cozy bar in front, instead of drifting apart.
- [x] **Merit badges — achievements for doing the real work** (2026-06-26) — your profile (cache journey window) now has a wall of collectible merit-badge medallions you earn by doing real money work: Awakened (named your cache), Connected (linked an account), Earner (tagged income), Architect (built a budget), Watchful (surfaced subscriptions), Vault Verified (ledger audited), Ascendant (Lvl 5), Blessed (max health), Steadfast (3-month streak), Devoted (30 days), Legend (Lvl 10), and the one-of-one **Founder** badge (machine-locked to the founder). Earned badges are flat **duotone 8-bit pixel tiles** (inked-in; the one-of-one Founder/Legend tiles invert to filled); locked ones show what to do; tap any badge for its how-to; new earns get written to your ledger so the pride is permanent. NEXT: a compact badge row on the sidebar card + a little earn celebration (sparkle/toast) the moment one unlocks.
- [x] Recurrence v2 — detect_recurring now flags **new** (first seen <10wk), **changed** (latest charge >10% off the usual), **dropped** (well past its usual gap). Shown as badges in the Money Map; dropped (tracked, not paused) surface in the Review inbox with "mark paused" to clear (2026-06-23)

## Parked / revisit
- [ ] PDF statement import — native macOS PDFKit extraction works (no installs), but some bank PDF layouts scramble date↔transaction mapping (one tested layout gave ≈99 amounts / 11 dates), unreliable for money. Use CSV export instead. Revisit only if a bank's PDF has a clean tabular layout.
- [ ] Cards without SimpleFIN support — import via CSV (drag onto board, or import_statements.py), or revisit a direct connect
- [x] Commit income tagger + SOPs to git — pushed to github.com/cozykace/thecache (data stays gitignored) (2026-06-23)

## Shipped
- [x] **Auto-clean merchant names** — `prettify_merchant()` turns raw bank descriptions into readable names for DISPLAY only (strips "Web Authorized Pmt", card/network noise, trailing state codes, collapses "Google Google" dupes, drops corp suffixes). Keys/tags untouched, so renames still override. Applied to recurring, deposits, income sources (2026-06-23)
- [x] **Averages widget** — lifetime monthly averages from the ledger: avg income, avg spend, avg net/shortfall (the deficit between inflow and outflow), avg gig income, avg subscriptions, avg spend/day, over N months (skips the partial current month). /api/averages (2026-06-22)
- [x] **Subscriptions "build your box"** — tracked list always shows anything tagged a subscription (even few charges); active/lapsed/paused pip read from the ledger's last-charge date (green=charged <40d, amber=lapsed >1mo, grey=manually paused — click pip to pause/reactivate, paused drops from the total); × to untrack; "+ add" links a merchant by name; "+ track" promotes a detected one. detect_recurring always includes tagged subs (2026-06-22)
- [x] **Settings panel** (Menu → ⚙ Settings) — Profile (name/role/note → time-based greeting in the sidebar); Money targets (reserve, monthly need, work rate, centralized from the inline prompts, ripple via Store); Display (Privacy blur = blur all dollar amounts until hover for screen-sharing; theme swatches). Stored in localStorage (2026-06-22)
- [x] **Ledger → JSONL (one transaction per line, append-only)** — migrated ledger.json (single object) → ledger.jsonl. Adding a txn is now an O(1) append (never rewrites history); a corrupt line is skipped individually instead of taking down the whole file; still plain text you can open. Auto-migrates the old format once (keeps ledger.json.pre-jsonl.bak), merge compacts only on updates, backup-restore handles both formats. 532 txns migrated + verified, consistency + live sync confirmed (2026-06-22)
- [x] **Ledger durability hardening** — (1) anti-wipe: a corrupt/unreadable ledger.json no longer silently falls back to empty (which let the next sync overwrite all history with the last 90 days); `load_ledger()` restores from the newest good backup, or refuses to write; `merge_ledger` has a shrink-guard. (2) count-based (multiset) CSV dedup so genuinely-repeated transactions (two same-price coffees same day) are kept instead of dropped, with unique per-occurrence ids. All tested; no regression on already-imported statements (2026-06-22)
- [x] **Single source of truth + accuracy fix** — (1) one client `Store` holds the canonical data; every money widget subscribes and renders from the same object; any edit (categorize, income tag, core/flex, period, sync) calls `Store.refresh()`/`Store.emit()` and EVERY widget re-renders together — edits ripple, widgets can't disagree. (2) Transfers / credit-card payments no longer counted as spending anywhere (was ~30% inflation + Months/Breakdown mismatch); period_summary, Months, and balances now use one consistent rule; broadened transfer detection (mobile pymt, e-payment, etc.); "Where it's going" shows excluded-transfers as a footnote. Verified income/spend/net identical across all paths for every month (2026-06-22)
- [x] Balance widget redesign — headline is now Total cash, with Checking and Savings rows beneath (split by account type, they sum to the total) + "as of" timestamp; skull still expands the full per-account list (2026-06-22)
- [x] Global Period selector — one date-range control in the dock (This month default · 30d · 90d · pick a month · All time) drives every "span" widget (income, spending, gap, core/flex, subs, work) off the full ledger via /api/summary; each span widget shows its period as a chip; balance/forecast stay point-in-time. Date range is now obvious everywhere (2026-06-22)
- [x] Categorizer shows transaction dates — each merchant row shows its date (or first–last range + count for repeats) under the name (2026-06-22)
- [x] The Dock — unified all bottom controls (scale, date/time, status, soundtrack, roadmap, sources, server, sync) into one cohesive bottom-center bar with a "dock" label; drag bubbles to reorder, toggle them in Menu → Dock (2026-06-22)
- [x] Date/time bubble added to the dock (live clock + date) (2026-06-22)
- [x] Version moved to the sidebar bottom in accent text; removed the conflicting footer (2026-06-22)
- [x] Brand dot next to the THE CACHE wordmark now mirrors live status (green/amber/red) like the dock server light (2026-06-22)
- [x] Categorizer window now opens centered, is draggable (by its header) and resizable (size persists) (2026-06-22)
- [x] Widgets resize from any of the 4 corners (top edges re-anchor); bottom-right corner indicator rounded to match the widget (2026-06-22)
- [x] Categorizer modal resizable (drag the corner, size persists) + auto-save hardened with saved/error feedback (2026-06-22)
- [x] Goat: removed trotting sprite; 8-bit goat head lives in the server light — spins when live, eyes X-out when offline/stale (2026-06-22)
- [x] Always-on server — moved the app out of TCC-locked Documents + a LaunchAgent that auto-starts on login & self-heals (KeepAlive). No more manual starts (2026-06-22)
- [x] Restart button — click the HUD server light to restart the backend in place (/api/restart self-execs); reloads when it's back (2026-06-22)
- [x] Inline income status edit — click the ✓/dot pip on a badge to confirm-income or mark-not-income, no modal (2026-06-22)
- [x] Core subscriptions feed into The Gap / need (subs you mark core now count toward what you must cover) (2026-06-22)
- [x] Untagged-deposit nudge — status indicator shows "Review N untagged deposits" so new income doesn't slip by (2026-06-22)
- [x] Merchant dedup — strip card-network prefixes so "Visa Apple Bill" merges with "Apple Bill" (2026-06-22)
- [x] Documented income-decision precedence (tag > gig/payroll hint > not-transfer & not-fee) in store.py (2026-06-22)
- [x] Data contract in CLAUDE.md — balances.json shape, data files, API, localStorage registry (2026-06-22)
- [x] /new-tagger skill — the override→decision→recompute→endpoint→modal recipe (2026-06-22)
- [x] check.sh — one-command self-check (JS/py syntax + logic sanity, counts only) (2026-06-22)
- [x] Pixel-goat sprite — extracted 8 trot frames from the reference video, knocked out the white bg, packed into goat-sprite.png; replaces the line-art goat (2026-06-21)
- [x] Backgrounds picker (✨ top-right) — Motion / Retro / Stickers categories, spins out, remembered; separate from theme (2026-06-21)
- [x] More extreme themes — Vapor, Acid, Ember palettes (2026-06-21)
- [x] Sync button → green arcade button (cutout text, 3D side, depresses on press) (2026-06-21)
- [x] Balance skull state persists across refresh; Months side padding re-evaluated (2026-06-21)
- [x] Bug report system — report → solve/reopen/delete, logged locally to data/bugs.json (Menu → Report a bug) (2026-06-21)
- [x] Resizable side menu — drag the right edge; width persists (2026-06-21)
- [x] Months redesign — bigger, labeled in/out bars with values + a hero net number (2026-06-21)
- [x] Status pills — more padding from the corner so they're not jammed in (2026-06-21)
- [x] Cursor — snappier follow (less lag) + smaller ball; Months widget — much larger bars & numbers (2026-06-21)
- [x] Bold display wordmark (thick, expanded, AKIRA-style); fit-to-menu so it no longer clips (2026-06-21)
- [x] Hold-space to pan the canvas — open-hand cursor that grabs closed on drag (2026-06-21)
- [x] Fix: income tagger / categorizer "backend not running" message now points to restarting (start.command) — it fired on a stale server missing /api/deposits (2026-06-21)
- [x] HUD backend light — live / restart-needed / offline status in the status bar, pinged every 8s; click for how to start (2026-06-21)
- [x] Snap gutter — grid-adjacent widgets now leave an 8px gap instead of touching (2026-06-21)
- [x] Utilities category + add-your-own categories from the categorizer (custom categories persist and flow through the whole system) (2026-06-20)
- [x] start.command — double-click launcher so the local backend is easy to start (2026-06-20)
- [x] Import account-matching — imported CSVs merge into the right live account by last-4 (Card No. column or filename), so history extends one account instead of splitting (2026-06-20)
- [x] Data coverage view — sources panel shows the live-sync window + per-account date range/count/source; Months show per-month txn count + source (2026-06-20)
- [x] Months view — see every backlogged month (income/spend/net + category drill-down) rolled up from the full ledger (2026-06-20)
- [x] /fix-bug skill — repeatable diagnose→fix→verify→log flow with this app's gotchas baked in (2026-06-20)
- [x] Fix: roadmap modal flicker when toggling Roadmap/Features — cached tabs + min-height so it no longer collapses and reveals the dashboard behind (2026-06-20)
- [x] Status indicator — bottom-right pill shows the top next action; click to see all (sync, confirm income, categorize, mark subs) (2026-06-20)
- [x] Subscription detail + rename — click a sub to see what it's connected to; rename it (alias only, data untouched) (2026-06-20)
- [x] FEATURES.md + Features tab in the roadmap — running product feature list for launch (2026-06-20)
- [x] In-dashboard CSV import — drag a CSV onto the board, or Menu → Import statement (2026-06-20)
- [x] CSV statement importer — drop bank CSVs in data/statements/, run import_statements.py (deduped, merged, local) (2026-06-20)
- [x] Roadmap button — bullet list of this backlog, read live from BACKLOG.md (2026-06-20)
- [x] Zoom + scroll the sandbox — pannable, scalable canvas (zoom control + ⌘/ctrl-wheel) (2026-06-20)
- [x] Income status pip — ✓ confirmed vs faint auto-dot on income sources; click a badge to edit (2026-06-20)
- [x] Income tagger — define what counts as income (2026-06-20)
- [x] Income as Lucide-icon knockout badges (2026-06-20)
- [x] Subscriptions widget — aggregate + core/flex per sub (2026-06-20)
- [x] Date ranges on windowed widgets (2026-06-20)
- [x] Per-widget magnet snap, 24px grid (2026-06-20)
- [x] CLAUDE.md conventions + /new-widget skill (2026-06-20)
